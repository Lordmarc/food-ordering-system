<div id="{{ $id ?? '' }}" class="shadow-md h-32 w-full p-4 rounded-md border">
    <h2 class="text-gray-500 text-md">{{ $title }}</h2>
    <p class="text-2xl font-bold amount">{{ $amount }}</p>
</div>
